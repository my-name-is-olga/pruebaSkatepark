const { Router } = require("express");
const router = Router();
const { pool } = require("../db");
/* const {
    //aquí va la funcion de admin controllers
} = require("../controllers/admin.controllers"); */

module.exports = router;
